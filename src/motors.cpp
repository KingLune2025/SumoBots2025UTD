#include <header.h>

void setMotorSpeeds(double leftSpeed, double rightSpeed) {

  // Code to actually set the motor speeds would go here
  // For example:
  // analogWrite(LEFT_MOTOR_PIN, leftMotorSpeed);
  // analogWrite(RIGHT_MOTOR_PIN, rightMotorSpeed);
}